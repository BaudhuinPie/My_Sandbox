const tu = { a: 'Tu', b: 'Vous' } ;
const soi = { a: 'Il', b: 'Elle', c: 'On'} ;
const soint = { a: 'Ils', b: 'Elles'} ;

const sujet = ['Je' , tu, soi, 'Nous', 'Vous', soint ] ;

const être = (suj, s, x = 0 ) => { 
    var su = '' ;
    switch (s) {
        case 1 : 
        case 5 :
            break ;
        case 2 :
            break ;
    }
    case

if (s < suj.length) {
    if (!(suj[s].x == 'undefined')) {
        if ( suj[s])
    } else {
         su = suj[s].x ;
    }
}
   
    }
    switch(suj[s])`${suj} suis`, `${suj} es`, `${suj} est`, sommes', 'êtes', 'sont'] ;
const aller = (suj) => ['vais', 'vas', 'va', 'allons', 'allez', 'vont'] ;

const evSuf = ['iens', 'iens', 'ons', 'ez','iennent' ];
const erSuf = [ 'e', 'es', 'e', 'ons', 'ez', 'ent'] ;

const devenir = (suj, ev = 'enir') => `${suj} dev${ev} ` ;
const rencontrer = (suj, er = 'er') => `${suj} rencontr${er} ` ;
const parler = (suj, er = 'er') => `${suj} parl${er} ` ;
const joindre = 



